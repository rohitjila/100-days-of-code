	def matSearch(self,mat, N, M, X):
	    i=0
	    j=M-1
	    while (i < N and j>=0):
	        if (mat[i][j])==X:
	            return 1
	        elif (mat[i][j])>X:
	            j-=1
	        else:
	            i+=1
	    return 0
