import math
def primeCheck(x):
    flag = 1
    for i in range(2,int(math.sqrt(x))+1):
        if(x % i==0):
            flag=0
            print("Not Prime")
            break
    if(flag==1):
        print("Prime")
num = int(input("Enter the number: "))
ret = primeCheck(num)
    
    
