def ans(a,b):
    return ((a*b)+(a-b))
def array(a,b):
    arr.sort()
    mx1=ans(arr[1],arr[0])
    mx2=ans(arr[n-1],arr[n-2])
    mx3=ans(arr[n-1],arr[0])
    print(max(mx1,mx2,mx3))
t=int(input())
for i in range(t):
    n=int(input())
    arr=list(map(int,input().split()))
    array(arr,n)
